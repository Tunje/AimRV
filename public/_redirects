# Redirect all routes to index.html for SPA
/* /index.html 200

# Handle asset paths
/assets/* /assets/:splat 200
/AimRV/assets/* /assets/:splat 200

# Ensure image paths work
/images/* /images/:splat 200
/videos/* /videos/:splat 200
